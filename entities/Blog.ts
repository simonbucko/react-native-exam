export class Blog {
    constructor(public title: string, public text:string, public published: string,
        public likeCount: string, public commentCount: string, public publisher:string, public id?: string) { }
}

